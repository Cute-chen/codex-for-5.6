# Codex.app 26.616.71553 Build 4265

## Summary

This note records the inspected bundle shape for the newly supported `Codex.app` build.

- Version: `26.616.71553`
- Build: `4265`
- Verification date: `2026-06-23`
- Result: `supported`

## Verified Feature Paths

- Composer `/fast` slash command
- Settings-side Fast control
- Composer `Intelligence` dropdown Speed submenu
- Plugins catalog and install paths for custom API users, with the old sidebar/page/detail API-key gates absent in the inspected bundle
- Curated OpenAI plugin catalog visibility, plugin install-button availability, plugin detail app-connect content, install-modal content, post-install app connect, shared plugin marketplace prefetch gates, and local full plugin cache root
- Settings > General `Disable automatic updates` row and dynamic background Sparkle check suppression through the launcher main-process hook
- Official GPT-5.5 model-list visibility is expected through the app path used by `26.422.30944` and later builds

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- The inspected target files are renamed from build `4212`: `general-settings-U7DFIZBC.js`, `composer-DlMDPaCL.js`, `use-service-tier-settings-DDtqCMIO.js`, `check-plugin-availability-DSvDEMVw.js`, `use-plugin-install-flow-DDuwrr2c.js`, `use-plugins-Dex8E4w_.js`, and `app-prefetch-impl-CtdRcCKu.js`.
- The shared settings schema for renderer General settings is in `webview/assets/src-l0hbMZ-p.js`; the main-process schema copy is in `.vite/build/src-DBVh5FZA.js`. Both still define `preventSleepWhileRunning`, which is the adjacent schema target used to add `disableAutomaticUpdates`.
- The General power settings row is still in `general-settings-U7DFIZBC.js` and matches the `Disable automatic updates setting` target.
- The Sparkle background-check hook moved to `.vite/build/workspace-root-drop-handler-DYf1cfzL.js`.
- The Settings-side Fast gate still destructures `isServiceTierAllowed` from the availability hook and preserves `serviceTierSettings.availableOptions.length<=1`.
- The shared service-tier allowance hook still treats `isServiceTierAllowed` as ChatGPT-only by default, and the conversation fallback still gives conversation-level and latest-turn `serviceTier` state priority over Settings before patching.
- The `/fast` slash command is still generated from service-tier command entries in the composer bundle.
- The composer `Intelligence` Speed menu still uses the code-level `composer.openModelPicker` target and a boolean `allowed && serviceTierSettings.availableOptions.length>1` gate.
- The old Plugins sidebar disabled-nav target, `/skills` page-content auth block, and plugin detail redirect target were not present in the inspected bundle.
- Curated OpenAI plugin catalog visibility is still hidden for non-ChatGPT auth by the `openai-curated-marketplaces-hidden` helper in `use-plugins-Dex8E4w_.js`.
- The `4218407052` vertical-catalog branch can still exclude the `openai-curated` marketplace after `list-plugins` returns it.
- The local full plugin cache query root target still matches, so `~/.codex/.tmp/plugins` remains included as a marketplace root for the full local `openai-curated` catalog.
- Plugin install availability still derives aggregate `connector-unavailable` state after a per-connector loop.
- Plugin detail app rows still enrich directory apps with plugin category metadata before filtering.
- Plugin install modal content still gates basic modal details through a disclosure boolean.
- The post-install flow now closes for `n == null && authPolicy === "ON_USE"` before considering app auth, and its single-app auto-open guard checks `authPolicy === "ON_INSTALL" || n != null`. The post-install app-connect target accepts this callback-shaped variant so `ON_USE` plugins with pending app auth stay in the connect flow.
- Startup plugin prefetch still requests the remote `shared-with-me` marketplace in `app-prefetch-impl-CtdRcCKu.js`.

## Verification Notes

- Direct bundle inspection reported guarded matches for Settings Fast, service-tier allowance, service-tier conversation fallback, service-tier slash command, composer `Intelligence` Speed through the code-level target, plugin install availability, plugin detail app connect, plugin install modal content, post-install app connect fallback/apps, shared plugin marketplace prefetch, the plugin catalog marketplace filter, and the plugin catalog local-cache target.
- Direct bundle inspection reported needle-only matches for the older post-install app-connect close and auto-open targets until the callback-shaped `26.616.71553` signatures were added.
- Direct bundle inspection reported guarded matches for the automatic-update settings schema and for the General settings row. The launcher-side main-process hook target moved to `.vite/build/workspace-root-drop-handler-DYf1cfzL.js`.
- Direct bundle inspection did not find the old Plugins sidebar/page/detail API-key gates, so runtime launch does not require the `Plugins access` target for this build.
- Regression coverage includes the `26.616.71553` (`build 4265`) whitelist gate, the no-legacy-Plugins-required runtime launch rule, and the updated callback-shaped post-install app-connect target.
