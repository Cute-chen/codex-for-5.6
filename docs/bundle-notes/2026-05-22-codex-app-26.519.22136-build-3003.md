# Codex.app 26.519.22136 Build 3003

## Summary

This note records the inspected bundle shape for the newly supported `Codex.app` build.

- Version: `26.519.22136`
- Build: `3003`
- Verification date: `2026-05-22`
- Result: `supported`

## Verified Feature Paths

- Composer `/fast` slash command
- Settings-side Fast control
- Composer `Intelligence` dropdown Speed submenu
- Plugins sidebar access for custom API users
- Plugins page content, plugin detail deep links, install-button availability, and install-modal content gates
- Official GPT-5.5 model-list visibility is expected through the app path used by `26.422.30944` and later builds

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- The inspected target files are `general-settings-BZV2EA65.js`, `composer-C6IpJrmm.js`, `app-main-BIL8Sji8.js`, `skills-page-HlnQvZ8j.js`, `plugin-detail-page-Rk9OBOvV.js`, `check-plugin-availability-6p9UsIaB.js`, and `use-plugin-install-flow-B_gXb-Jd.js`.
- The Settings-side Fast gate now uses `xe()` for availability and `fe()` for the service-tier hook. The early return also checks `serviceTierSettings.availableOptions.length<=1`; the patch removes only the Fast availability guard and preserves the option-count fallback.
- The old single `/fast` command target is gone. Slash commands are now generated from service-tier options in `composer-C6IpJrmm.js`; the patch force-enables the generated service-tier command entries.
- The composer `Intelligence` Speed submenu now renders through `W=v&&h.availableOptions.length>1?...:null`. The patch removes `v&&` while preserving the option-count guard.
- The Plugins sidebar disabled-nav state now uses `Li("533078438")` with `Cc(authMethod)`.
- The unified Skills/Plugins nav label state now uses `bs({hostId:Tt})` and an API-key exclusion.
- The `/skills` route page-content gate still matches the existing API-key-derived local assignment shape.
- The plugin detail redirect still reads `authMethod` and redirects under the existing auth-helper guard.
- The plugin install availability gate remains in `check-plugin-availability-*.js`.
- The plugin install modal content gate moved to `use-plugin-install-flow-*.js`.
- The model-list bridge still delegates to `model/list`, but `codexfast` skips GPT-5.5 apply targets on this build because GPT-5.5 is expected to be visible through the official app path from `26.422.30944` onward.

## Gate Notes

- The Settings-side Fast control uses `n=xe(),{serviceTierSettings:r,setServiceTier:i}=fe();if(!n||r.availableOptions.length<=1)return null;`.
- The service-tier slash command entries use `requiresEmptyComposer:!1,enabled:n`.
- The composer `Intelligence` Speed menu renders only when the local `v` result is truthy and service-tier options contain more than one entry.
- The Plugins sidebar disabled-nav state uses `d=e&&l&&u`.
- The unified Skills/Plugins nav label state uses `p=e&&f&&!u`.
- The Plugins page content state still uses the existing API-key-derived local assignment shape.
- The plugin install availability helper derives the top-level blocked reason from `connector-unavailable`; the patch keeps only the `disabled-by-admin` aggregate block.
- The plugin install modal content gate derives from plugin detail apps and `ON_INSTALL` auth policy; the patch forces that local gate false so basic plugin information can render normally.

## Verification Notes

- Direct bundle inspection confirmed the supported feature paths are still present with renamed asset files and updated Settings Fast, slash command, Intelligence Speed, and Plugins sidebar shapes.
- Regression coverage includes the `26.519.22136` (`build 3003`) whitelist gate and runtime patch behavior for the Settings, service-tier slash command, Intelligence Speed, sidebar Plugins, Plugins page content, plugin detail, plugin install availability, and plugin install modal content target shapes.
