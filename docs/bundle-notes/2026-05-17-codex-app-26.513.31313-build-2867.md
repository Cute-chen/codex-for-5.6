# Codex.app 26.513.31313 Build 2867

## Summary

This note records the inspected bundle shape for the newly supported `Codex.app` build.

- Version: `26.513.31313`
- Build: `2867`
- Verification date: `2026-05-17`
- Result: `supported`

## Verified Feature Paths

- Composer `/fast` slash command
- Settings-side Fast control
- Composer `Intelligence` dropdown Speed submenu
- Plugins sidebar access for custom API users
- Plugins page content, plugin detail deep links, install-button availability, and install-modal content gates
- Composer `@` plugin mentions for local and already available plugin results under API-key auth
- Official GPT-5.5 model-list visibility is expected through the app path used by `26.422.30944` and later builds

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- The inspected target files are `general-settings-DWGOfOIB.js`, `composer-DXaiOlFj.js`, `app-main-DZOIl7aU.js`, `skills-page-BCITaFO4.js`, `plugin-detail-page-CL4xxcbU.js`, `check-plugin-availability-BLUA-GwE.js`, `plugins-availability-DpWDTiPu.js`, and `prosemirror-PI_17HLA.js`.
- The Settings-side Fast gate still uses `se()` for availability and `fe()` for the service-tier hook.
- The Fast slash command remains in a `composer-*.js` asset and still uses the existing `enabled:<local>` target shape.
- The composer `Intelligence` Speed submenu still renders through an inline `ve=v?...:null` assignment, but the rendered component local changed from `KR` to `qR`.
- The Plugins sidebar disabled-nav state now uses `ec("533078438")` with `Xl(authMethod)`.
- The unified Skills/Plugins nav label state now uses `Nl({hostId:Qo})` and an API-key exclusion.
- The `/skills` route page-content gate still matches the existing API-key-derived local assignment shape.
- The plugin detail redirect still uses `if(ue(i))` after `authMethod` is read.
- The plugin install availability gate remains in `check-plugin-availability-*.js`.
- The plugin install modal content gate remains in `plugins-availability-*.js`.
- The composer `@` plugin mention list now sets `additionalMarketplaceKinds:s?[\`shared-with-me\`]:[]`, where `s` is the `N("1269116100")` feature flag result. The patch changes this to `s?[]:[]`; it does not add remote ChatGPT catalog support.
- The model-list bridge still delegates to `model/list`, but `codexfast` skips GPT-5.5 apply targets on this build because GPT-5.5 is expected to be visible through the official app path from `26.422.30944` onward.

## Gate Notes

- The Settings-side Fast control uses `n=se(),{serviceTierSettings:r,setServiceTier:i}=fe();if(!n)return null;`.
- The Fast slash command still uses `enabled:a`.
- The composer `Intelligence` Speed menu renders only when the local `v` result is truthy.
- The Plugins sidebar disabled-nav state uses `d=e&&l&&u`.
- The unified Skills/Plugins nav label state uses `p=e&&f&&!u`.
- The Plugins page content state uses `let p=f` after the API-key auth helper result.
- The plugin detail page redirect uses `if(ue(i))`.
- The plugin install availability helper derives the top-level blocked reason from ``j=_.length>0&&O===_.length?D?`disabled-by-admin`:`connector-unavailable`:null``; the patch keeps only the `disabled-by-admin` aggregate block.
- The plugin install modal content gate derives from plugin detail apps and `ON_INSTALL` auth policy; the patch forces that local gate false so About, Includes, and Capabilities can render normally.
- The composer `@` mention list sets `c={additionalMarketplaceKinds:s?[\`shared-with-me\`]:[]}` before calling the plugin list hook; the patch uses `s?[]:[]`.

## Verification Notes

- Direct bundle inspection confirmed the supported feature paths are still present with renamed asset files and updated Intelligence Speed, Plugins sidebar, and composer plugin mention shapes.
- Regression coverage includes the `26.513.31313` (`build 2867`) whitelist gate and apply/restore behavior for the Settings, Intelligence Speed, sidebar Plugins, Plugins page content, plugin detail, plugin install availability, plugin install modal content, and composer plugin mention target shapes.
