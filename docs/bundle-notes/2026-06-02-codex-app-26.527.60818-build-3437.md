# Codex.app 26.527.60818 Build 3437

## Summary

This note records the inspected bundle shape for the newly supported `Codex.app` build.

- Version: `26.527.60818`
- Build: `3437`
- Verification date: `2026-06-02`
- Result: `supported`

## Verified Feature Paths

- Composer `/fast` slash command
- Settings-side Fast control
- Composer `Intelligence` dropdown Speed submenu
- Plugins sidebar access for custom API users
- Plugins page content, plugin detail deep links, install-button availability, and install-modal content gates
- Official GPT-5.5 model-list visibility is expected through the app path used by `26.422.30944` and later builds

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- The inspected target files are `general-settings-Bt2lh7rT.js`, `composer-CwxGJF3C.js`, `app-main-BwpsB7rB.js`, `skills-page-BIZqKbZI.js`, `plugin-detail-page-uf22h4TJ.js`, `check-plugin-availability-CDJyCxkN.js`, and `use-plugin-install-flow-DpUQcozA.js`.
- The Settings-side Fast gate still uses `je()` for availability and `_e()` for the service-tier hook. The early return still checks `serviceTierSettings.availableOptions.length<=1`; the patch removes only the Fast availability guard and preserves the option-count fallback.
- The `/fast` slash command is still generated from service-tier command entries in the composer bundle.
- The composer `Intelligence` Speed menu still uses the option-count ternary shape patched by the existing `intelligence-speed-menu-options-ternary` target.
- The Plugins sidebar disabled-nav state still uses `xa("533078438")`, but the API-key auth helper changed from `wl(authMethod)` to `lc(authMethod)`.
- The unified Skills/Plugins nav label state now uses `hc({hostId:mr})` with the same API-key exclusion pattern that must be removed.
- The `/skills` route page-content gate, plugin detail redirect, plugin install availability, and plugin install modal content gates still match the existing target shapes.
- The composer `@` plugin mention file still contains the plugin loading UI, but no `shared-with-me` marketplace request was present in the inspected target files.

## Verification Notes

- Direct bundle inspection reported guarded matches for the Settings Fast, service-tier slash command, Intelligence Speed, Plugins sidebar, Plugins page content, plugin detail, plugin install availability, and plugin install modal content targets.
- Regression coverage includes the `26.527.60818` (`build 3437`) whitelist gate plus runtime patch behavior for the new Plugins sidebar target shape.
