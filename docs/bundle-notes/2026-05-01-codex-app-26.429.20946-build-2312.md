# Codex.app 26.429.20946 Build 2312

## Summary

This note records the inspected bundle shape for the newly supported `Codex.app` build.

- Version: `26.429.20946`
- Build: `2312`
- Verification date: `2026-05-01`
- Result: `supported`

## Verified Feature Paths

- Composer `/fast` slash command
- Settings-side Fast control
- Composer `Intelligence` dropdown Speed submenu
- Plugins sidebar access for custom API users
- Official GPT-5.5 model-list visibility is expected through the app path used by `26.422.30944` and later builds

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- The inspected target files are `general-settings-BBebpJo8.js`, `composer-B5UwBne4.js`, `index-Dyn_7Tv1.js`, and `use-model-settings-D_GIIENF.js`.
- The Settings-side Fast gate uses `de()` for availability and `Ve()` for the service-tier hook.
- The Fast slash command moved from `index-*.js` to `composer-B5UwBne4.js` and still uses the existing `enabled:<local>` target shape.
- The composer `Intelligence` Speed submenu moved to `composer-B5UwBne4.js` and now gates rendering through `qs(...)`.
- The Plugins sidebar disabled-nav state still derives from the `533078438` experiment and custom API auth state, but the helpers changed to `ms("533078438")` and `ed(authMethod)`.
- The unified Skills/Plugins nav label now derives from `g({hostId:Or})&&!E`.
- The model-list bridge still delegates to `model/list`, but `codexfast` skips GPT-5.5 apply targets on this build because GPT-5.5 is expected to be visible through the official app path from `26.422.30944` onward.

## Gate Notes

- The Settings-side Fast control still uses the guarded `if(!n)return null;` shape.
- The Fast slash command still uses `enabled:a`, where `a` is derived from `qs(...)`.
- The composer `Intelligence` Speed menu renders only when the local `qs(...)` result is truthy.
- The Plugins sidebar disabled-nav state uses `D=T&&E`.
- The unified Skills/Plugins nav label state uses `te=g({hostId:Or})&&!E`.

## Verification Notes

- Real installed-app status detection initially reported the build as unsupported before the whitelist update.
- Direct bundle inspection confirmed the supported feature paths are still present with moved target filenames and updated gate signatures.
- Real installed-app apply patched four targets, created `app.asar1`, updated the Electron ASAR integrity hash, re-signed successfully, and reset the Codex screen-recording permission record.
- Real installed-app status after apply reported Settings Fast, composer `Intelligence` Speed menu, `/fast`, and Plugins access as enabled.
- Regression coverage includes the `26.429.20946` (`build 2312`) whitelist gate and apply/restore behavior for the new Settings, Intelligence Speed, and Plugins target shapes.
