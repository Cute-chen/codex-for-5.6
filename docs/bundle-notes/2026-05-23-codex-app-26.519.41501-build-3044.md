# Codex.app 26.519.41501 Build 3044

## Summary

This note records the inspected bundle shape for the newly supported `Codex.app` build.

- Version: `26.519.41501`
- Build: `3044`
- Verification date: `2026-05-23`
- Result: `supported`

## Verified Feature Paths

- Composer `/fast` slash command
- Settings-side Fast control
- Composer `Intelligence` dropdown Speed submenu
- Plugins sidebar access for custom API users
- Plugins page content, plugin detail deep links, install-button availability, and install-modal content gates
- Official GPT-5.5 model-list visibility is expected through the app path used by `26.422.30944` and later builds

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- The inspected target files are `general-settings-D_BmV12Y.js`, `composer-D0cvMZjq.js`, `app-main-DG-Mf4Wj.js`, `skills-page-C8PW4EqX.js`, `plugin-detail-page-jAJa26RM.js`, `check-plugin-availability-6p9UsIaB.js`, and `use-plugin-install-flow-IT_xMrDV.js`.
- The target mapping matches `26.519.22136` (`build 3003`) with renamed assets.
- The Settings-side Fast gate still uses `xe()` for availability and `fe()` for the service-tier hook, with the `serviceTierSettings.availableOptions.length<=1` fallback guard preserved by the patch.
- The `/fast` slash command is still generated from service-tier command entries in the composer bundle.
- The composer `Intelligence` Speed menu still uses the option-count ternary shape patched by the existing `intelligence-speed-menu-options-ternary` target.
- The Plugins sidebar disabled-nav state still uses `Li("533078438")` with `Cc(authMethod)`.
- The unified Skills/Plugins nav label state still uses `bs({hostId:Tt})` and an API-key exclusion.
- The `/skills` route page-content gate, plugin detail redirect, plugin install availability, and plugin install modal content gates still match the existing 26.519 target shapes.

## Verification Notes

- Direct bundle inspection reported guarded matches for the Settings Fast, service-tier slash command, Intelligence Speed, Plugins sidebar, Plugins page content, plugin detail, plugin install availability, and plugin install modal content targets.
- Regression coverage includes the `26.519.41501` (`build 3044`) whitelist gate through the runtime-launch flow test.
