# Codex.app 26.623.31443 Build 4441

## Summary

This note records the inspected bundle shape for the newly supported `Codex.app` build.

- Version: `26.623.31443`
- Build: `4441`
- Verification date: `2026-06-26`
- Result: `supported`

## Verified Feature Paths

- Composer `/fast` slash command
- Settings-side Fast control
- Composer `Intelligence` dropdown Speed submenu
- Settings > General `Disable automatic updates` row, plus dynamic background Sparkle check suppression through the launcher main-process hook
- Official Plugins support through the app path, with codexfast Plugins runtime targets skipped for this build
- Official GPT-5.5 model-list visibility is expected through the app path used by `26.422.30944` and later builds

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- Settings-side Fast moved to `general-settings-Lfz-Yitn.js` and still uses the destructured `isServiceTierAllowed` gate with `serviceTierSettings.availableOptions.length<=1`.
- The shared service-tier allowance and conversation fallback targets moved to `app-initial~app-main~onboarding-page~profile-Y_KHAkrf.js`.
- The conversation fallback still uses the `!==void 0` service-tier shape. The patch must normalize the next-turn service-tier check to `!=null` plus `!==\`standard\`` so explicit Fast survives, while latest-turn `params.serviceTier` is ignored and null/Standard state falls back to Settings.
- The request service-tier helper moved to `app-initial~app-main~remote-conversation-page~onboarding-page~hotkey-window-thread-page~ava~igqafzkz-BamPzidr.js` and still gated non-ChatGPT auth methods with `:!1`; the patch must preserve ChatGPT `fast_mode` checks but allow other auth methods to return a configured Fast tier.
- The `/fast` slash command and composer `Intelligence` Speed menu target moved to `app-initial~app-main~remote-conversation-page~new-thread-panel-page~appgen-library-page~hot~djo67r4n-TIbmYSpe.js`.
- The automatic-update Settings row moved to a new `La()` function shape in `general-settings-Lfz-Yitn.js`, covered by `disable-automatic-updates-setting-26623`.
- The automatic-update settings schema target matches `.vite/build/child-process-snapshot-worker.js`.
- The launcher-side Sparkle hook target is in `.vite/build/workspace-root-drop-handler-Cejfos9U.js`.
- The old Plugins sidebar disabled-nav target, `/skills` page-content auth block, and plugin detail redirect target were not present in the inspected bundle.
- Several older Plugins catalog/install/app-connect signatures are still detectable by the generic target inspector, but this build supports Plugins through the official app path. Runtime launch filters out all Plugins target ids for `26.623.31443+4441`.

## Verification Notes

- `plutil` reported `CFBundleShortVersionString` `26.623.31443` and `CFBundleVersion` `4441` from `/Applications/Codex.app/Contents/Info.plist`.
- Direct bundle inspection reported guarded matches for Settings Fast, service-tier allowance, service-tier request allowance, service-tier conversation fallback, service-tier slash command, composer `Intelligence` Speed through the code-level target, the automatic-update schema target, and the `disable-automatic-updates-setting-26623` General settings target.
- Direct bundle inspection did not find the old Plugins sidebar/page/detail API-key gates.
- Regression coverage includes the `26.623.31443` (`build 4441`) whitelist gate, the no-legacy-Plugins-required runtime launch rule, and the runtime patcher wrapper that skips Plugins target ids while preserving non-Plugins targets for this build.
